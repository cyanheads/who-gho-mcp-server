---
summary: "Metadata alignment: Dockerfile restored to oven/bun:1.3, scripts migrated from tsx to bun, .env.example restructured, tsx devDependency removed"
breaking: false
security: false
---

# 0.1.5 — 2026-05-23

## Changed

- **`Dockerfile`**: restored `oven/bun:1.3` as build stage (reverts earlier `node:24-slim` workaround); build step now uses `bun run build` instead of `npm run build`; LABEL description updated to match canonical tagline.
- **`package.json`**: all script entries migrated from `tsx`/`npm run` to `bun run`; `files[]` trimmed to remove `changelog/` and `AGENTS.md` entries; `start:stdio` and `start:http` updated to `bun ./dist/index.js`.
- **`manifest.json`**: description updated to canonical tagline; formatting normalized (inline objects expanded).
- **`CLAUDE.md`**: commands table updated to `bun run` equivalents; bundling and publishing sections rewritten to match current workflow.
- **`.env.example`**: restructured with grouped sections, full option coverage, and inline annotations.
- **`.gitignore`**: aligned to current mcp-ts-core template (added Python, Java, Ruby sections; OS/archive/compiled file patterns).
- **`tsx`**: removed from `devDependencies` (scripts invoked via `bun run`, shebang was vestigial).
